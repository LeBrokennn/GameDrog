# URL base del backend (sin barra final). Las rutas relativas de imagen se pegan aquí.
REACT_APP_API_BASE=http://localhost:8080
# Ruta del listado de productos (ajusta si tu API usa otro path)
REACT_APP_PRODUCTOS_PATH=/api/productos
